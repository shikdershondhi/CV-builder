interface:
  display_name: "CV Builder"
  short_description: "Repo-specific patterns and workflows for CV-builder"
  default_prompt: "Use the CV-builder repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true